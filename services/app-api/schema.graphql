type SignedUrl {
	url: String!
	key: String!
	fields: String!
}

type Api {
	key: String!
}

type Query {
	signedUrl: SignedUrl!
	api: Api!
}

#type Mutation {
#	createNote(note: NoteInput!): Note
#	deleteNote(noteId: String!): String
#	updateNote(note: UpdateNoteInput!): Note
#}
